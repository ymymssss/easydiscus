# ymopenclawuse

This repository is used by OpenClaw to store non-sensitive artifacts (notes, exported files, skill drafts).

- Do NOT commit secrets (tokens, passwords, private keys).
- Prefer adding new items under `uploads/` or `skills/`.
